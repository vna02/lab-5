#include <iostream>
#include <math.h>
using namespace std;

int main() {
	int y;
cout<<"enter a year here:";
if (y%4==0)
{
cout<<"the year is leap year";
}
else
{
cout<<"the year is not a leap year";
}
	return 0;
}
